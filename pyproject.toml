[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "programmer-simulator"
version = "0.1.0"
description = "A small programming simulator game."
requires-python = ">=3.11"
dependencies = [
  "customtkinter>=5.2",
]
